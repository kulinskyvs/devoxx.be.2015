Modules packade restrictions demo

* Demostrate that it's possible to have multiple modules that exports the same package if they don't depend on each other
 * compile.sh
 * run.sh

* Demostrate **runtime** error when trying to launch module that with adding another module that exports the same package
 * uncomment corresponding line in run.sh
 * run.sh

* Demostrate **runtime** error when trying to launch module jar from classpath with addind abother module that exports the same package to modulepath
 * uncomment corresponding line in run.sh
 * run.sh